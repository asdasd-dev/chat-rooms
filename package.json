{
  "name": "chat-rooms",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd root/backend && npm start\" \"cd root/frontend && npm start\"",
    "deps-front": "cd root/frontend && npm install",
    "deps-back": "cd root/backend && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
